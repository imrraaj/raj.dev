---
export interface Props extends astroHTML.JSX.AnchorHTMLAttributes {}

const { href, class: className, ...props } = Astro.props;
const isActive = href === Astro.url.pathname.replace(/\/$/, "");
---

<a
  href={href}
  class={`inline-flex justify-between items-center gap-2 px-2 mr-2 rounded-sm  text-lg font-semibold   ${
    isActive ? "bg-primary text-background " : "text-accent hover:text-primary"
  }`}
  {...props}
>
  <slot />
</a>
